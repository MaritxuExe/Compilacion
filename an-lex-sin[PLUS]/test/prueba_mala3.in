def main () # prueba mala 3

 
let _a_b, b, c : int; #! ERROR: no se admite _ al principio. Lo dará el análisis sintáctico
 d___f, e : float;      #! ERROR: no se admiten tres _ consecutivos. tomará d _ _ _ f. _ NO SE DEBE METER EN EL LÉXICO COMO TOKEN

  def prozesatu(x: int; y: int)  # dos parámetros por valor
    let lag, bueltak_, $emaitza, emaitza__1: int;
    	f : float in
    '''' subprogramas imbricados
     ''  controlar bien luego, utilizando la tabla de símbolos '''
  {
    def gehibat (x: & int)  {
      x = x + 1;
    } # end
    def kenbat (x: & int) {
      x = x - 1;
    } # end
 
    lag = y; $emaitza = x;
    forever {
      break if emaitza__1 < 0 ;
      emaitza__1 = emaitza__1 - 1 * bueltak_ // y;
    } # bucle infinito 

    if $emaitza < 1000 {
      bueltak_ = 0;
      do {
	# gehibat($emaitza); '''  para tratar llamadas ?? a subprogramas '''
	if $emaitza > 100000 { next; }
	# kenbat(lag);	   ''' para tratar llamadas ** a subprogramas '''
	bueltak_ = bueltak_ + 1;
      }  until lag != 0 			# ERROR: tiene que aparecer /= 
      else { print($emaitza); }  #! do-until
   
      if emaitza__1 == 0000 { print(emaitza__1); } # ERROR: no puede haber ceros a la izquierda de números enteros
      if emaitza__1 > 0 { print(bueltak_); }
    } #! if amaiera
  
    print(bueltak_);
  } #! end prozesatu

  def gehibi (x: & int)  { # parametro por referencia
    x = x + 2;
  } #! amaiera

'''*** aqui comienza el  ** / *
       programa principal ***''''

  read(a); read(b); read(f);
  e = 02.6743E2; # ERROR: no puede haber ceros a la izquierda en números reales
  print(e);
  e = e * 4.900e+02; # ERROR: no puede haber ceros a la izquierda de los exponentes en números reales
  print(e);
  # gehibi(f);
  e = e - 0.5;
  d = 1/e;
  println(d);
  # prozesatu(a,b);   ''''' Los símbolos que hay después NO SON NUEVOS TOKENS, estan dentro de un comentario * ** &%$·""?¿ñ^[]Çç:;-_,.\ | ! @   **** ***
  c = c*(c*d)+e;
  print(c); print(f);
  
  # ERROR: varios símbolos incorrecos, NO SON NUEVOS TOKENS y no se deben aceptar!! No se deben meter en el léxico!!
  &%$·""?¿ñ^[]Çç:;-_,.\ |! # @ 

} # end main


